# Common environment variables (shell-agnostic)
# Format: KEY=VALUE (no spaces around =, no export keyword)
# These are parsed by all shells (fish, bash, zsh)

# Editor & Tools
EDITOR=emacs
VISUAL=emacs

# Add more common vars here as you migrate them
# PATH additions should go in path.env
